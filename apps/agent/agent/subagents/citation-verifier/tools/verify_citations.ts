// Re-export root tool into this subagent's namespace (Slice 7.2). See sibling
// verify_identifiers.ts. `.ts` ext required (node v25 native TS).
export { default } from "../../../tools/verify_citations.ts";
