/**
 * Ritme spasial changelog — sejajar dengan blog & chrome marketing supaya gutter X konsisten.
 * `changelogColumn` = lebar baca + center; `changelogGutter` = padding X (px-5 → sm:px-6 → lg:px-8).
 */
export const changelogColumn = 'mx-auto w-full max-w-3xl';
export const changelogGutter = 'px-5 sm:px-6 lg:px-8';

/** Easing fisik (anti ease-in-out generik) untuk transisi changelog. */
export const changelogEase = 'ease-[cubic-bezier(0.32,0.72,0,1)]';
