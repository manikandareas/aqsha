# Local/emergency BUILD overlay for compose.yaml.
#
# Normal prod path builds the images in CI and pushes to GHCR; compose.yaml only references them by
# `image:`. When you need to build on the host instead (CI down, air-gapped bring-up, debugging a
# Dockerfile), overlay this file — it re-adds the `build:` blocks. Because each service keeps its
# compose.yaml `image:` ref, a local build TAGS the image with that same GHCR name, so a subsequent
# `up` uses the freshly built image without pulling:
#
#   docker compose -f compose.yaml -f compose.build.yaml build
#   docker compose up -d            # (compose.yaml alone — runs the just-built images)
#
# PUBLIC_SITE_URL / PUBLIC_SEO_ALLOW_INDEXING / GIT_COMMIT come from the shell env or an `.env` next
# to compose. Source-map upload to Sentry stays OFF locally (no SENTRY_AUTH_TOKEN) — CI-only concern.
services:
  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
      args:
        # Only the SEO prerender bake happens at build time; all other app config is injected at
        # runtime by the Infisical entrypoint (skipped locally → the app's built-in defaults apply).
        PUBLIC_SITE_URL: ${PUBLIC_SITE_URL:-https://aqshara.com}
        PUBLIC_SEO_ALLOW_INDEXING: ${PUBLIC_SEO_ALLOW_INDEXING:-false}
        GIT_COMMIT: ${GIT_COMMIT:-local}

  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
      args:
        GIT_COMMIT: ${GIT_COMMIT:-local}

  agent:
    build:
      context: .
      dockerfile: apps/agent/Dockerfile
      args:
        GIT_COMMIT: ${GIT_COMMIT:-local}
