# Bun-only trusted skill scripts

Aqsha uses Bun with TypeScript or JavaScript as the single v1 runtime for trusted skill scripts in Deep Research sandboxes. This keeps script execution, schema validation, dependency management, testing, and Daytona packaging aligned with the API-owned agent runtime, while visual artifacts are generated through JavaScript visualization libraries instead of handwritten SVG generators or arbitrary model-generated plotting code.
