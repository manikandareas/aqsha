# Deep Review — Implementasi Agent Mastra (Astra)

**Tanggal:** 2026-07-02
**Cakupan:** Runtime core (`apps/agent/src/mastra`) → tools & workflow `/deep` → integrasi frontend streaming/durability (`apps/web`).
**Metode:** 3 sub-review paralel + verifikasi manual ke source `node_modules` terpasang (`@mastra/core@1.47`, `@mastra/memory@1.21`, `@mastra/server@1.47`, `@mastra/client-js@1.28`, `@mastra/fastembed@1.2`) + cross-check docs resmi Mastra.
**Re-verifikasi (2026-07-02):** seluruh 64 temuan diverifikasi ulang secara independen oleh 6 agen adversarial (tiap agen diminta **membantah** temuan, bukan mengonfirmasi), menelusuri seluruh rantai kausal ke source aktual + kode framework terpasang. Hasil: **0 false alarm** — 61 CONFIRMED penuh, 3 PARTIAL (temuan nyata, satu detail mekanisme dikoreksi). Semua marker `🔎` dinaikkan ke `✅`. Lihat banner di bawah.

## Legenda

| Simbol | Arti                                                                                                    |
| ------ | ------------------------------------------------------------------------------------------------------- |
| 🔴     | Critical — segera                                                                                       |
| 🟠     | High — dampak nyata ke kualitas/kepercayaan                                                             |
| 🟡     | Medium — perlu diperbaiki, tak mendesak                                                                 |
| 🟢     | Low / polish                                                                                            |
| ✅     | Terverifikasi langsung ke source (semua marker `🔎` dinaikkan ke `✅` setelah re-verifikasi 2026-07-02) |
| ⚠️     | Temuan CONFIRMED tapi ada koreksi mekanisme/dampak dari re-verifikasi (lihat catatan `[Verif 07-02]`)   |
| 🔎     | (deprecated) Basis bukti sweep — tak lagi dipakai setelah re-verifikasi menyeluruh                      |
| ✔️     | Sudah diperbaiki — detail di section "Progress perbaikan"                                               |

Kolom **Keputusan / Saran** dikosongkan untuk diisi owner.

---

## Hasil re-verifikasi (2026-07-02)

**64 temuan, 0 false alarm.** Setiap temuan ditelusuri ulang ke source aktual + `node_modules/@mastra/` terpasang. Semua klaim "primitive tersedia tanpa upgrade" (bagian E) terkonfirmasi ada di versi terpasang.

**3 temuan PARTIAL — nyata, tapi satu detail dikoreksi:**

- **DUR-5** — benar run `running` tak resume & poll tak berguna. Koreksi: "menyerah senyap setelah 8 error (~20s)" **hanya** saat `runById` throw (server down). Jika agent restart lalu server hidup lagi, `runById` sukses baca snapshot beku, `errors` reset ke 0, dan poll **loop selamanya** (shimmer macet permanen) — bukan menyerah.
- **DUR-6** — benar `queueMessage` tak dipakai & tak ada antrean. Koreksi: "pesan diam-diam hilang" **overstated** — composer sudah busy-aware (`canSend = … && !busy`), submit early-return saat busy; teks tetap tersimpan di input, tak lenyap. Gap sebenarnya = fitur queueing tak ada, bukan message loss.
- **IMP-13** — 2 dari 3 sub-klaim benar (`strip-mention-markers.ts:10` klaim strip history **faktual salah**; komentar `memory.ts:25` abai bobot tool payload). Sub-klaim `astra-lite.ts:62` tak terbukti menyesatkan — komentarnya wajar & `TokenLimiter` memang jalan per-step.

**Temuan yang ternyata LEBIH parah dari tulisan awal (di-_under_-state):**

- **SEC-1** — prasyarat exploit (token tanpa email claim) adalah **kondisi default** session Clerk (`packages/chat-core/src/index.ts:39` menandai email "best-effort, bukan klaim token standar"). Jadi broadly exploitable, bukan corner case. `mergeBodyRequestContext` (`@mastra/server` `chunk-XRE4KFIZ.js:88`) hanya blok key `mastra__`; `aqsha__email` lolos. Naikkan prioritas eksekusi ke paling atas.
- **CFG-5** — search **berbayar** (`external_search`, 2 kredit) bisa jalan **sebelum** gate billing `approve-plan` (satu-satunya titik debit). Plus `delete_artifact` (requireApproval-suspend) melanggar aturan repo sendiri (`tools/index.ts:61-64`) yang melarang suspend-tool dalam workflow-generate.
- **FE-1 / FE-2** — history fetch selalu pakai `ASTRA_AGENT_ID="astra-lite"`, jadi thread **Pro pun terpotong di 16 pesan** (bukan 32). Flip lite→pro saat refresh mid-run inilah yang bikin FE-2 (duplikasi teks) **deterministik**, bukan sekadar probabilistik.

**Nuansa mitigasi (tak membatalkan temuan):** CTX-2 cache `"failed"` per-cacheKey expire 12 mnt (retry per query unik setelah TTL, tapi silence + charge-tanpa-refund tetap nyata); CFG-7/CFG-9 sebagian "defended" oleh rationale terdokumentasi (`citation_verify` 0-kredit; debit turn aman via replay buffer) — risiko laten ke path masa depan; FE-10 landmine laten (Hono tak kompres by default, semua call site GET/POST).

---

## Progress perbaikan

| ID    | Status                     | Catatan implementasi                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ----- | -------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SEC-1 | ✔️ DONE 2026-07-02 | `userContextMiddleware` kini pre-seed SEMUA key server-owned `aqsha__*` (daftar baru `AQSHA_SERVER_OWNED_CONTEXT_KEYS` di `lib/tool-context.ts`) dengan `""` sebelum handler — `mergeBodyRequestContext` skip key yang sudah terdefinisi, jadi injeksi klien (email/agent_kind/deep_run_id/dll.) tak pernah masuk; semua reader perlakukan `""` sebagai absen. Email terverifikasi Clerk di-set setelahnya bila ada. Verifikasi: typecheck hijau + script vs `RequestContext` asli + replika merge framework (8 asersi PASS, termasuk baseline vulnerable & set server-side pasca-merge tetap jalan). AWAS: key `aqsha__*` baru WAJIB ditambah ke daftar itu. |
| CTX-1 | ✔️ DONE 2026-07-02 | Jalur `/deep`: output tool riset TANPA `n` (`toResearchToolOutput({numbered:false})` saat `deepRunId` ada) — satu-satunya sumber `[n]` kini inventory global `assign-citations`. Instruksi `literature-searcher`/`counter-evidence` diganti kontrak identifier (DOI/arXiv/URL, "NEVER write [n]"); `searcherPrompt` + `synthesisPrompt` + `deep-writer` selaras (writer memetakan klaim→inventory via identifier/judul). Jalur chat tetap bernomor global per-turn (tak berubah). |
| CTX-2 | ✔️ DONE 2026-07-02 | Provider (openalex/arxiv/crossref×2/firecrawl) kini return `ProviderSearchResult` diskriminatif (`ok:false reason:"provider_error"` + message); cache `failed` TIDAK lagi disajikan sebagai hasil kosong — dilaporkan sebagai error backoff; misconfig FIRECRAWL_API_KEY tak di-cache `failed` (pulih instan). Tool riset pakai alur baru `runResearchTool`: gate kuota non-consuming (`requireEntitlement`) → provider → **debit hanya saat provider sukses** → note eksplisit ke model utk 3 kasus (kuota habis / provider gagal "kredit tidak dipotong" / benar-benar kosong). Caller Explore waterfall + CitationService diadaptasi; test provider di-update + 2 test regresi cache-failed baru. |
| CTX-3 | ✔️ DONE 2026-07-02 | Embedder Memory fastembed (BGE-small-en) → OpenAI `text-embedding-3-small` via `ModelRouterEmbeddingModel` (`@mastra/core/llm`), kredensial DISAMAKAN dgn embedding RAG app (`AQSHA_EMBEDDING_API_KEY ?? OPENAI_API_KEY`, `AQSHA_EMBEDDING_BASE_URL`) → SATU model embedding utk seluruh app (RAG sudah text-embedding-3-small sejak awal). Dep `@mastra/fastembed` dihapus. CATATAN: index vector Mastra diturunkan dari dimensi (384→1536) → pesan lama tak ter-recall semantik sampai re-embed (jendela `lastMessages` tetap utuh); re-embed massal = ops terpisah bila dibutuhkan. |
| CTX-4 | ✔️ DONE 2026-07-02 | `stripMentionMarkersProcessor` kini juga implement `processInputStep` — strip marker U+E000/E001 atas SEMUA pesan yang dikirim ke LLM tiap step (termasuk history/recall yang masuk via `messageList.add(msg,"memory")` tanpa lewat `processInput`). Aman: pesan sumber `memory` tak ikut `drainUnsavedMessages` (DB tetap ber-marker utk pill FE); idempoten. Sisa DITERIMA: query embedding recall masih dihitung dari teks mentah (tak ada titik cegat bersih di Mastra 1.47; polusi 2 char PUA/mention, minor). |
| CTX-5 | ✔️ DONE 2026-07-02 | `instructions` astra-lite/pro kini DynamicArgument async: inti statis per-tier + blok `## Konteks sesi` di AKHIR (prefix tetap ter-cache) — tanggal hari ini (id-ID, Asia/Jakarta, + instruksi pakai tanggal ini utk "terbaru"/rentang tahun), nama user (`UserRepo`), paket (`getEntitlementSnapshot`, re-export baru dari `@aqsha/services/billing`), tier agen. Lookup best-effort (2 query ringan; gagal → tetap tanggal+tier). |
| CTX-6 | ✔️ DONE 2026-07-02 | Processor baru `ElideHistoryToolResultsProcessor` (`processInputStep`, dipasang SEBELUM TokenLimiter): payload hasil tool ≥1.5k char pada pesan asisten bersumber `memory` diganti stub `{elided, toolName, note}` (note arahkan model panggil ulang tool bila perlu). Pesan turn AKTIF + 1 pesan asisten riwayat terbaru dibiarkan utuh. DB tak tersentuh (memory-source tak di-persist ulang); idempoten. |
| CTX-7 | ✔️ DONE 2026-07-02 | `maxSteps` eksplisit per subagent (`literature-searcher` 8, `counter-evidence` 10, `citation-verifier` 4, `deep-writer` 12) + instruksi "ALWAYS end with final text". Guard teks kosong di workflow: `searchStep` retry SEKALI dgn reminder lalu fallback "(Sub-pertanyaan GAGAL diriset…)"; `counter-evidence`/`verify-citations` fallback teks jujur (bukan string kosong senyap ke sintesis). |
| CTX-8 | ✔️ DONE 2026-07-02 | Output tool riset kini bawa `authors` (maks 3), `year`, `venue` dari `metadataJson` provider (`candidateCitationMeta`, ekspor baru `@aqsha/services/research`); kolom baru `research_sources.authors_json/year/venue` (mig **0025**, sudah di-migrate dev) di-persist saat search → inventory bernomor `assign-citations` menyertakan `(authors; year; venue)` → `verify_identifiers` + daftar pustaka APA bekerja dari metadata asli. |
| CFG-1 | ✔️ DONE 2026-07-02 | Dua SKILL.md dikoreksi ke kontrak tool NYATA: `synthesis-matrix` → tawarkan lewat percakapan lalu `propose_artifact` SEKALI dgn `{title, markdown}` (tak ada `artifactType`/`planBullets`/`execute_artifact`); `verify-citations` → tool `verify_citations` + `artifactText` (muat teks artifact dulu via `get_render_payload`). `bun run skills:gen` di-run. Sweep tambahan (permintaan owner): SEMUA referensi `eve` di source dihapus/reword self-contained (29 file) — komentar historis porting, `/eve` di `robots.ts` disallow (route mati), prefix ID test `eve:` → `astra:`, tipe `EvePrincipal` → `SessionPrincipal` (chat-core, hanya dipakai internal file itu); docs/ dibiarkan (arsip). Verifikasi: grep `\beve\b` 0 match di seluruh source. |
| CFG-2 | ✔️ DONE 2026-07-02 | `synthesisPrompt` kini meng-INLINE panduan (langkah synthesize `toolChoice:"none"` → instruksi "baca skill lewat tool" tak pernah bisa jalan): domain-pack `research-<domain>` + `write-academic-id` + `cite-apa7` (di-scope "hanya penyebutan naratif; sitasi inline tetap [n]"). Konten dari `inlineSkillInstructions(name)` (helper baru `skills.ts`, SSOT tetap SKILL.md via codegen). `domain` dipilih planner di `draft-plan` (field baru JSON plan, enum medicine/cs-ml/education/general, fallback general). Kontradiksi "daftar sumber" direkonsiliasi: instruksi `deep-writer` + `deep-research/SKILL.md` kini sama dgn prompt (penanda [n] → pill + panel Sumber; JANGAN tulis daftar sumber/daftar pustaka sendiri). |
| CFG-3 | ✔️ DONE 2026-07-02 | Edit plan-gate kini RE-DERIVE `subQuestions`: `approve-plan` dgn `resumeData.edits` → satu pass murah `deepWriter` (`replanPrompt`, `toolChoice:"none"`) menulis ulang plan + sub-pertanyaan + domain dari suntingan user, emit ulang detail `plan` ke FE; gagal parse/throw → fallback perilaku lama (append prosa, subQuestions lama). Kontrak JSON plan di-share `PLAN_JSON_CONTRACT` (planPrompt+replanPrompt). |
| CFG-4 | ✔️ DONE 2026-07-02 | Isolasi kegagalan per-sub-Q di `searchStep`: badan fan-out di-try/catch → subagent yang throw (429/timeout terminal) menghasilkan findings gap jujur (teks "GAGAL diriset"), bukan menolak seluruh `Promise.all` → run tetap selesai dgn hasil parsial (kredit sudah didebit di plan-gate). `retries: 1` pada step LLM pasca-billing: `counter-evidence`, `verify-citations`, `synthesize` (+ `draft-plan` pra-billing); `search-literature` SENGAJA tanpa retries (re-run = re-debit `external_search`; isolasi per-sub-Q sudah mencegah throw). SISA (tak dibangun): refund `deep_research` saat run terminal `failed` — butuh API refund di BillingService yang belum ada; jalur failure pasca-billing kini tinggal synthesize/assign-citations gagal 2×. |
| CFG-5 | ✔️ DONE 2026-07-02 | `draft-plan` kini `toolChoice:"none"` (sejajar draft-clarify/synthesize) → search berbayar tak bisa jalan sebelum gerbang billing `approve-plan`, dan `delete_artifact` (approval-suspend) tak bisa terpanggil dalam workflow-generate. Semua pemanggilan `deepWriter` di Workflow kini tanpa tool (replan CFG-3 juga `none`). |
| CFG-6 | ✔️ DONE 2026-07-02 | Debit turn chat (`billing.ts`) kini resolve `threadId` via `resolveOwnerThread(requestContext, messages)` (pola `thread-projection`) — bukan `MASTRA_THREAD_ID_KEY` yang tak pernah diset di jalur chat → ledger `provider_usage_ledger.thread_id` terisi, view usage per-thread hidup. |
| CFG-7 | ✔️ DONE 2026-07-02 | `verify_citations` + `verify_identifiers` kini menghormati boolean `chargeToolUsage`: gate gagal → return `{error:"quota_exhausted", note}` TANPA menjalankan verifikasi (laten-proof bila `citation_verify` kelak diberi rate >0). Refund-on-provider-error jalur search sudah beres via CTX-2 (`runResearchTool` debit-on-success). |
| CFG-8 | ✔️ DONE 2026-07-02 | Helper baru `fetchWithRetry` (`packages/services/src/papers/http.ts`): SATU retry ber-jitter (400–1200ms) pada 429/5xx atau error jaringan; timeout/abort & 4xx lain TIDAK di-retry. Dipakai keempat provider riset (openalex, crossref×2, arxiv, firecrawl); pacing arXiv tetap. |
| CFG-9 | ✔️ DONE 2026-07-02 | Debit turn chat kini membawa `idempotencyKey` turn-scoped = `<id pesan asisten>:<feature>` (id respons dari `messages` processor output; tanpa id → tanpa key, perilaku lama). Path retry masa depan (error processor / re-drive / streamUntilIdle) tak bisa double-debit senyap. |
| CFG-10 | ✔️ DONE 2026-07-02 (partial by-design) | `EnsureFinalResponseProcessor` trigger `stepNumber >= maxSteps-1` (bukan `===`) → caller yang MENAIKKAN `maxSteps` per-call tetap dapat reminder tiap step sejak cap konstruktor (regresi turn-senyap tertutup). DIVERIFIKASI ke `@mastra/core@1.47` terpasang: `ProcessInputStepArgs` TIDAK mengekspos `maxSteps`/`stopWhen` efektif → cap runtime tak bisa dibaca processor; sisa laten terdokumentasi = caller yang MENURUNKAN cap di bawah nilai konstruktor (reminder tak fire) — jangan kirim `maxSteps` per-call lebih kecil. |
| CFG-11 | ✔️ DONE 2026-07-02 | `TokenLimiterProcessor` kini `{ limit: tokenLimit, trimMode: "contiguous" }` — pemangkasan kontigu dari pesan terlama, tak lagi melubangi tengah percakapan / memisahkan tool-call dari result-nya. |
| FE-1 | ✔️ DONE 2026-07-02 | Fetch history seed kini `listMessages({ perPage: 400 })` (`HISTORY_SEED_PER_PAGE`, `mastra-chat-thread-surface.tsx`). CATATAN verifikasi ke server terpasang: `perPage` HARUS angka — route mem-parse `z.coerce.number` (`perPage:false` via HTTP → 400); server ambil N TERBARU lalu dibalik → kronologis. Lewat 400 pesan butuh paginasi sungguhan (belum dibangun, di luar cakupan). |
| FE-2 | ✔️ DONE 2026-07-02 | Filter dedup replay per-run di `onChunk` (`createChunkReplayFilter`, `use-mastra-agent.ts`): pencocokan PREFIX signature (`type:JSON(payload)`) per `runId`; chunk `start` = kandidat awal segmen (dicari di applied → replay resume dari titik itu; tak ketemu → segmen kelanjutan baru, mis. resume tool-suspend dgn runId sama). Run terminal dibuang dari map. Seen-counter polos SENGAJA tidak dipakai — resume suspend memakai runId sama sehingga `start` kedua akan salah terdeteksi replay. Guard doc-edit lama dipertahankan (lapis kedua efek berbayar). |
| FE-3 | ✔️ DONE 2026-07-02 | Latch per-mount di `MastraChatThreadSurface` (adjust-state-during-render): first-paint ditahan sampai refetch on-mount React Query selesai → seed inner selalu data segar saat kunjung ulang; refetch background setelah tampil tidak memblok. |
| FE-4 | ✔️ DONE 2026-07-02 | `stop()` kini bercabang `/deep`: `deepRunRef.current.cancel()` (cancel run Workflow server-side; `cancel()` diverifikasi ada di client-js 1.28) + clear runId (poll re-attach tak menghidupkan ulang) + settle & clear gate; jalur chat tetap `subscription.abort()`. |
| FE-5 | ✔️ DONE 2026-07-02 | Poll re-attach kini dipicu ulang tanpa refresh: `reattachNonce` (useReducer) jadi dep effect poll; catch `sendDeep`/`resolvePlan`/`resolveAsk` → `clearDeepRunIdUnlessAlive` return bool, run hidup → bump nonce TANPA banner error; plus `maybeReattachAfterStreamClose` menangkap stream yang tertutup tanpa chunk terminal & tanpa gerbang HITL (blip senyap). |
| FE-6 | ✔️ DONE 2026-07-02 | Chunk `error` chat dibedakan by TEKS jawaban (bukan output apa pun): ada teks → settle + banner "mungkin terpotong, Buat ulang"; belum ada teks (baru tool-call/reasoning) → banner error penuh. Tool-row saja tak lagi membuat kegagalan senyap. |
| FE-7 | ✔️ DONE 2026-07-02 | `settleWorkflowTurn(state, runId)` baru (ekspor `mastra-timeline`): settle pesan ber-`turnId === runId`, status "ready" hanya bila tak ada turn lain streaming; fallback positional hanya bila run tak pernah ter-tag DAN tak ada run lain aktif. Dipakai `workflow-finish`/`workflow-canceled`/`error` (reducer workflow) + semua settle jalur poll re-attach. |
| FE-8 | ✔️ DONE 2026-07-02 | `lastSendRef` (mode/text/clientContext/richText/attachmentIds/agentKind) diisi `send`/`sendDeep`; regenerate yang match turn terakhir sesi: chat → kirim ulang `richText` + `clientContext` via `ifIdle.streamOptions.context`; `/deep` → hapus pasangan server + `dropLastTurn` lokal lalu `sendDeep` ulang (bukan downgrade chat yang menghapus report). Setelah refresh → fallback teks polos (perilaku lama). |
| FE-9 | ✔️ DONE 2026-07-02 | Gate `!busy` dibuang dari `useThreadSources` (tinggal `messages.length > 0`) → invalidasi mid-run (search-literature / assign-citations) langsung refetch, kartu sumber DB-joined terisi selagi run berjalan. |
| FE-10 | ✔️ DONE 2026-07-02 | Proxy: `accept-encoding` masuk `DROP_REQUEST_HEADERS` (respons diteruskan tanpa `content-encoding` → upstream tak boleh kompres) + export `PATCH`/`PUT`/`DELETE` (client-js `thread.update`/`thread.delete` tak lagi 405). |
| FE-11 | ✔️ DONE 2026-07-02 | `useSendStatus("deep_research")` kini dipakai: pre-check di `onComposerSend` saat `command === "deep"` → toast blokir (pesan spesifik /deep per reason) sebelum Workflow jalan; invalidasi send-status pasca-kirim mencakup kedua feature. Backstop otoritatif tetap precheck server. |
| FE-12 | ✔️ DONE 2026-07-02 | Loop langganan: kegagalan di-log (`console.warn`; di-skip utk thread baru pra-kirim yang memang wajar gagal), ≥3 gagal beruntun → banner degraded (string konstan, otomatis dibersihkan saat tersambung lagi); `reconnect` internal client-js dibatasi `{maxRetries:20}` supaya outage panjang mid-session lolos ke loop luar dan terdeteksi (default Infinity = senyap selamanya). |

Verifikasi 2026-07-02: `bun run typecheck` hijau semua workspace; test services touched-files 39 pass (0 fail); billing itests 29 pass dgn `--timeout 25000` (2 fail di run default = pre-existing timeout DB Tailscale, terkonfirmasi juga fail pada tree bersih); api itests 81 pass. Migration **0025** sudah diterapkan ke DB dev (`bun run db:migrate`, di-run ulang idempoten utk konfirmasi). Belum di-restart: proses dev agent (wajib full-restart pasca `db:migrate`, gotcha reusePort). E2E owner: cek recall bahasa Indonesia (embedder baru), run `/deep` penuh (sitasi + guard), dan tanggal di sapaan.

Verifikasi section C (2026-07-02): `bun run typecheck` hijau semua workspace; test services tersentuh (fetch-timeout, citation-service, astra-chat-services, explore-service, deep-research-billing, chat) 24+52 pass dgn `--timeout 25000` (gotcha DB Tailscale). E2E owner utk C: `/deep` dgn edit plan-gate (cek sub-pertanyaan ikut berubah), `/matriks` (kontrak `propose_artifact` baru), dan cek ledger `thread_id` terisi di usage per-thread.

Verifikasi section D (2026-07-02): `bun run typecheck` hijau semua workspace; `bun run lint` 0 error (20 warning pre-existing di file lain); eslint langsung pada 4 file tersentuh (`use-mastra-agent.ts`, `mastra-timeline.ts`, `mastra-chat-thread-surface.tsx`, `mastra-api/[...path]/route.ts`) bersih. Klaim API diverifikasi ke `node_modules` terpasang: `run.cancel()` ada (client-js 1.28), `perPage` route di-coerce number (false → 400), replay langganan = buffer run aktif dari index 0 tanpa penanda urutan (core 1.47), reconnect internal client-js default Infinity. E2E owner utk D: (1) refresh mid-run thread Pro → teks TIDAK dobel; (2) Stop saat `/deep` → run benar-benar berhenti (cek status run + kredit); (3) chat → Explore → balik → turn terakhir langsung tampil; (4) matikan agent dev saat thread terbuka → banner degraded muncul, hidupkan lagi → banner hilang; (5) regenerate turn `/deep` → tetap jadi run `/deep`.

---

## A. Critical & Keamanan

| ID    | Prio  | Lokasi                                                                                                                               | Temuan                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Dampak (failure scenario)                                                                                                                             | Arah perbaikan                                                                                                                           | Keputusan / Saran |
| ----- | ----- | ------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- | ----------------- |
| SEC-1 | 🔴 ✅ ✔️DONE | `apps/agent/src/mastra/middleware/user-context.ts:28`; `lib/tool-context.ts:23`; sink `packages/services/src/billing/snapshot.ts:23` | **Billing admin bypass via client-injectable** `aqsha__email`**.** `@mastra/server` `mergeBodyRequestContext` menyalin semua key `requestContext` dari body/query klien yang bukan `mastra__`\_ (RESERVED). `aqsha\_\`\_\_ lolos. Middleware hanya set email saat token punya email claim (session Clerk default TIDAK) & tak pernah scrub. **[Verif 07-02: exploitability lebih tinggi — token tanpa email claim = kondisi DEFAULT Clerk (\*`\*chat-core/index.ts:39` **"best-effort, bukan klaim standar"). Broadly exploitable. Attacker cuma perlu tahu 1 admin email terkonfigurasi.]\*\* | User terautentikasi kirim `{requestContext:{"aqsha__email":"<admin>"}}` → `resolveAdminOverride`→`isAdmin:true`→ kredit unlimited + gate Pro terbuka. | Middleware **selalu** `set(AQSHA_EMAIL_KEY, verifiedEmail ?? "")` / delete sebelum handler; scrub semua `aqsha_`\_ sebagai server-owned. | approve. ✔️ **DONE 2026-07-02** — lihat "Progress perbaikan" di atas |

---

## B. Bug yang membuat agent "bodoh" / out of context (Q1)

| ID    | Prio  | Lokasi                                                                                                                               | Temuan                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Dampak                                                                                                                                                                       | Arah perbaikan                                                                                                                                                                             | Keputusan / Saran                                                                                 |
| ----- | ----- | ------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------- |
| CTX-1 | 🔴 ✅ ✔️DONE | `apps/agent/src/mastra/lib/research.ts:126`; `workflows/deep-research.ts:526-530`; `agents/literature-searcher.ts`                   | `/deep` **nomor sitasi bertabrakan lalu di-renumber diam-diam.** Tiap tool call balik `[1..k]` (offset 0), subagent disuruh pertahankan `[n]` PERSIS, `assign-citations` renumber global by `createdAt`, writer disuap inventory global + teks findings dengan `[n]` yang menunjuk sumber lain.                                                                                                                                                        | Writer menyalin `[2]` dari findings; `[2]` global = paper berbeda → **miscitation tak kelihatan** (pill resolve ke sumber salah). Merusak nilai jual anti-halusinasi.        | Output tool `/deep` tanpa `n` (atau ID per-subQ `S3-2`); writer hanya cite dari inventory global; atau renumber teks findings sebelum sintesis.                                            | approve. ✔️ **DONE 2026-07-02** |
| CTX-2 | 🔴 ✅ ✔️DONE | `packages/services/src/research/openalex.ts:117` (+arxiv/crossref/firecrawl `:27-31`); `research/cache.ts`; tools `search-papers.ts` | **Kegagalan provider = senyap, di-cache, tetap ditagih.** `catch{ writeCachedCandidates(...,[],"failed"); return [] }`; tool balik `{results:[]}` tanpa note; `readCachedCandidates` sajikan cache apa pun tanpa cek status (12 mnt). **[Verif 07-02: "mati permanen" sedikit longgar — cache** `failed` **per-cacheKey expire 12 mnt → re-attempt per query unik setelah TTL. Silence ke LLM + charge-before-search-tanpa-refund persis spt klaim.]** | Agent simpulkan "tak ada hasil". `FIRECRAWL_API_KEY` unset → `search_web` mati (per-query, sampai TTL) senyap. Kredit sudah dipotong per call termasuk retry ke cache-gagal. | Hasil diskriminatif `{ok:false, reason:"provider_error" "no_results"}`sampai output tool; skip/refund debit saat provider error; jangan sajikan cache`status:"failed"`.                    | approve. ✔️ **DONE 2026-07-02** |
| CTX-3 | 🟠 ✅ ✔️DONE | `apps/agent/src/mastra/memory.ts:54`                                                                                                 | **Semantic recall pakai embedder English (**`fastembed` **= BGE-small-en) untuk produk Indonesia.**                                                                                                                                                                                                                                                                                                                                                    | Recall lintas-turn (topK 6/12) lemah untuk percakapan ID → "linglung" di luar jendela `lastMessages`.                                                                        | Ganti ke multilingual (`multilingual-e5-large`) / embedding OpenAI (API key sudah wajib via `assertEmbeddingEnabled`). Catatan: index diturunkan dari dimensi → butuh re-embed pesan lama. | approve. gunakan model dari openai untuk semua embedding pada app ini. gunakan model text-3-small. ✔️ **DONE 2026-07-02** |
| CTX-4 | 🟠 ✅ ✔️DONE | `apps/agent/src/mastra/processors/strip-mention-markers.ts:10-33`                                                                    | **Marker** `@mention` **(U+E000/E001) bocor ke LLM di semua history.** Runner Mastra hanya lewatkan pesan BARU ke `processInput`; pesan history/recall masuk via `messageList.add(msg,"memory")` tak lewat processor. Query embedding recall juga dihitung dari teks belum-strip.                                                                                                                                                                      | Tiap turn, tiap pesan history ber-mention diputar dengan karakter PUA; recall query terpolusi; model bisa echo balik PUA.                                                    | Implement juga `processInputStep` strip atas `messageList.get.all.db()` (fungsi idempoten, aman). Strip query recall di sumber jika bisa.                                                  | approve. ✔️ **DONE 2026-07-02** |
| CTX-5 | 🟠 ✅ ✔️DONE | `apps/agent/src/mastra/instructions.ts:10`; `agents/astra-lite.ts:43`                                                                | **System prompt statis — tak tahu tanggal, user, plan.** Tak ada injeksi date/user di seluruh jalur chat.                                                                                                                                                                                                                                                                                                                                              | Diminta "penelitian terbaru" → pakai tahun basi, salah menilai kebaruan; tak bisa sapa nama / adaptasi tier.                                                                 | `instructions` sebagai `DynamicArgument` (`({requestContext})=>...`): suntik tanggal + nama + plan; blok dinamis di AKHIR agar prefix ter-cache.                                           | approve. ✔️ **DONE 2026-07-02** |
| CTX-6 | 🟡 ✅ ✔️DONE | `apps/agent/src/mastra/memory.ts:49-50`                                                                                              | **History window memutar full tool-result JSON tanpa pruning.** 16/32 pesan verbatim; tiap pesan asisten riset bawa seluruh hasil `search_`.                                                                                                                                                                                                                                                                                                           | 16 pesan tool-heavy = puluhan ribu token dump basi mengubur percakapan; TokenLimiter baru bertindak di 96k.                                                                  | `processInputStep` yang mengelide payload hasil tool pada pesan lama (simpan nama tool + ringkasan) sebelum TokenLimiter.                                                                  | approve. ✔️ **DONE 2026-07-02** |
| CTX-7 | 🟠 ✅ ✔️DONE | `agents/literature-searcher.ts`; `agents/counter-evidence.ts`; `workflows/deep-research.ts:732,750`                                  | **Subagent bisa selesai di langkah tool-call → teks kosong; bukti hilang senyap.** Default `stopWhen=stepCountIs(5)` [✅], subagent tak set `maxSteps`. `searchStep` ambil `findings:out.text` tanpa cek kosong.                                                                                                                                                                                                                                       | Bucket bukti / bagian adversarial kosong mengalir ke sintesis tanpa surfacing (bug "turn senyap" yang sudah difix di astra-lite tapi tidak di subagent).                     | `maxSteps` eksplisit per subagent + guard `if(!out.text.trim())` → retry/catat "sub-pertanyaan gagal".                                                                                     | approve. ✔️ **DONE 2026-07-02** |
| CTX-8 | 🟡 ✅ ✔️DONE | `apps/agent/src/mastra/lib/research.ts:87-112`                                                                                       | **Output tool riset membuang authors/year/venue.** `metadataJson` tersimpan di DB tapi tak ditunjukkan ke LLM.                                                                                                                                                                                                                                                                                                                                         | `verify_identifiers` minta authors/year tapi model tak pernah terima → langkah hampa; daftar pustaka APA → author/year dikarang.                                             | Sertakan `authors` (3 pertama), `year`, `venue` di output tool & inventory bernomor.                                                                                                       | approve. ✔️ **DONE 2026-07-02** |

---

## C. Misconfiguration / logika yang membuat agent tidak maksimal (Q2)

| ID     | Prio  | Lokasi                                                                                                          | Temuan                                                                                                                                                                                                                                                                                                                                                                                                                    | Dampak                                                                                                                           | Arah perbaikan                                                                                                                    | Keputusan / Saran                                           |
| ------ | ----- | --------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------- |
| CFG-1  | 🟠 ✅ ✔️DONE | `skills/synthesis-matrix/SKILL.md:21`; `skills/verify-citations/SKILL.md:6` (compiled `skills-inline.ts:53,58`) | **SKILL.md ajarkan kontrak tool eve lama.** synthesis-matrix suruh `propose_artifact`+`artifactType`/`planBullets` lalu `execute_artifact` (schema asli `{title?,markdown}`; `execute_artifact` TAK ADA). verify-citations suruh tool `verifyCitations`+`artifactId` (asli `verify_citations`+`artifactText`).                                                                                                            | `/matriks` → tool halusinasi/error loop. verify-citations → "no parseable references" → agent bilang bibliografi user kosong.    | Koreksi kedua SKILL.md + `bun run skills:gen`.                                                                                    | approve. makesure pada repo tidak ada lagi referensi ke eve ✔️ **DONE 2026-07-02** |
| CFG-2  | 🟠 ✅ ✔️DONE | `workflows/deep-research.ts:530` vs `:816-819`; `agents/deep-writer.ts:26-31`                                   | **Synthesis suruh writer load skill saat** `toolChoice:"none"` → APA 7/PRISMA/register akademik tak pernah load di langkah yang paling butuh. Plus kontradiksi "daftar sumber" (deep-writer suruh, synthesisPrompt larang).                                                                                                                                                                                               | Laporan akhir tanpa disiplin metodologi domain; struktur akhir acak.                                                             | Inline domain-pack + cite-apa7 ke `synthesisPrompt`; rekonsiliasi wording "daftar sumber".                                        | approve ✔️ **DONE 2026-07-02** |
| CFG-3  | 🟠 ✅ ✔️DONE | `workflows/deep-research.ts:699-702`                                                                            | **Edit plan-gate tak sampai ke sub-pertanyaan.** Edit user di-append sebagai prosa ke `plan`; `subQuestions` yang diriset tak berubah.                                                                                                                                                                                                                                                                                    | "Buang sub-pertanyaan 3 / fokus Indonesia" hanya dilihat writer akhir; fan-out tetap riset yang lama → affordance edit kosmetik. | Re-derive `subQuestions` (draft-plan pass murah) setelah edit sebelum searching.                                                  | approve ✔️ **DONE 2026-07-02** |
| CFG-4  | 🟠 ✅ ✔️DONE | `workflows/deep-research.ts:717-734`                                                                            | `searchStep` **fan-out** `Promise.all` **tanpa isolasi kegagalan; tak ada step retry.** Kredit dipotong di plan-gate; 1 rejection subagent menolak seluruh `Promise.all`.                                                                                                                                                                                                                                                 | 429/timeout 1 dari 3-6 subagent → run `failed`, tanpa refund, tanpa hasil parsial.                                               | `Promise.allSettled` + degradasi per-subQ + `retries:1-2` pada step LLM [✅ step support retries]; refund saat terminal `failed`. | approve ✔️ **DONE 2026-07-02** |
| CFG-5  | 🟡 ✅ ✔️DONE | `workflows/deep-research.ts:618-621`                                                                            | `draft-plan` **jalan dengan tools aktif** (tak ada `toolChoice:"none"` seperti draft-clarify/synthesize). `deepWriter` bawa `search_`(debit), writes, `delete_artifact`(requireApproval). **[Verif 07-02: LEBIH parah — search berbayar bisa jalan SEBELUM commit billing** `approve-plan` **(satu-satunya gate debit);** `delete_artifact` **approval-suspend langgar aturan repo sendiri** `tools/index.ts:61-64`**.]** | User bisa ditagih `external_search` untuk plan yang lalu ditolak; step gantung pada approval tanpa handler.                      | `toolChoice:"none"` di draft-plan.                                                                                                | approve ✔️ **DONE 2026-07-02** |
| CFG-6  | 🟡 ✅ ✔️DONE | `apps/agent/src/mastra/processors/billing.ts:62-63`                                                             | **Ledger** `threadId` **selalu** `undefined` **di jalur chat.** Baca `MASTRA_THREAD_ID_KEY` yang tak pernah diset di chat (thread ada di key `"MastraMemory"`).                                                                                                                                                                                                                                                           | View usage per-thread kosong.                                                                                                    | Pakai `resolveOwnerThread(requestContext, messages)` seperti `thread-projection.ts:80`.                                           | approve ✔️ **DONE 2026-07-02** |
| CFG-7  | 🟡 ✅ ✔️DONE | `apps/agent/src/mastra/lib/research.ts:27-48`; `verify-citations.ts:22`                                         | **Debit-before-result tanpa refund path.** Digabung CTX-2: kuota `external_search` Free tier bisa habis di provider mati tanpa 1 sumber. `verify-` abaikan boolean hasil `chargeToolUsage`.                                                                                                                                                                                                                               | Kuota terkuras di pencarian gagal/kosong; potensi bypass kuota jika `citation_verify` diberi rate.                               | Refund pada provider error; hormati return `chargeToolUsage`.                                                                     | approve ✔️ **DONE 2026-07-02** |
| CFG-8  | 🟡 ✅ ✔️DONE | `packages/services/src/papers/http.ts`; `research/pacer.ts`                                                     | **Tak ada retry/backoff pada API akademik flaky; hanya arXiv di-pace.** Run Pro 8-subQ fan-out paralel ke OpenAlex/Crossref/Firecrawl tanpa pacing/retry.                                                                                                                                                                                                                                                                 | 429 → jadi empty senyap (CTX-2).                                                                                                 | 1 retry + jitter pada 429/5xx di `fetch`.                                                                                         | approve ✔️ **DONE 2026-07-02** |
| CFG-9  | 🟢 ✅ ✔️DONE | `apps/agent/src/mastra/processors/billing.ts:66-76`                                                             | **Debit turn tanpa** `idempotencyKey` walau infra (`billing.service.ts:279`) & docstring mengharapkan. Aman sekarang (resume replay buffer).                                                                                                                                                                                                                                                                              | Double-debit senyap jika ada retry path masa depan (error processor, re-drive, `streamUntilIdle`).                               | Pass key turn-scoped (response message id / chatTurnId).                                                                          | approve ✔️ **DONE 2026-07-02** |
| CFG-10 | 🟢 ✅ ✔️DONE | `agents/astra-lite.ts:78-81,92`; `processors/ensure-final-response.ts:25`                                       | `EnsureFinalResponseProcessor(maxSteps)` **di-couple by-convention.** `defaultOptions` merge via deepMerge; caller kirim `maxSteps` per-call → geser cap, reminder tetap di `maxSteps-1`. Laten (FE cuma kirim `context`).                                                                                                                                                                                                | Reminder "tulis final" datang mid-run (truncation) atau tak pernah (regresi turn senyap).                                        | Turunkan step reminder dari cap efektif runtime, bukan konstruktor.                                                               | approve ✔️ **DONE 2026-07-02** |
| CFG-11 | 🟢 ✅ ✔️DONE | `agents/astra-lite.ts:86`                                                                                       | `TokenLimiterProcessor(number)` **default** `trimMode:'best-fit'` (non-kontigu, bisa melubangi tengah percakapan & pisahkan tool-call/result).                                                                                                                                                                                                                                                                            | Konteks berlubang tak koheren dekat batas token.                                                                                 | `{ limit: tokenLimit, trimMode:"contiguous" }`.                                                                                   | approve ✔️ **DONE 2026-07-02** |

---

## D. Frontend — bug correctness streaming & data

| ID    | Prio  | Lokasi                                                                           | Temuan                                                                                                                                                                                                                                                                                                                                                                                                                                     | Dampak                                                                                                  | Arah perbaikan                                                                                                          | Keputusan / Saran |
| ----- | ----- | -------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- | ----------------- |
| FE-1  | 🟠 ✅ ✔️DONE | `features/thread-experience/components/mastra-chat-thread-surface.tsx:110-113`   | **History seed kepotong 16 pesan.** `thread.listMessages()` tanpa `perPage` → server fallback `config.lastMessages` (16). **[Verif 07-02: fetch selalu pakai** `ASTRA_AGENT_ID="astra-lite"` **→ thread PRO pun terpotong 16, bukan 32.]**                                                                                                                                                                                                 | Thread > ~8 giliran kehilangan pesan lama setelah refresh, tanpa indikasi/paginasi.                     | Pass `perPage` eksplisit (atau `perPage:false`+`orderBy`) / paginasi.                                                   | approve. ✔️ **DONE 2026-07-02** |
| FE-2  | 🟠 ✅ ✔️DONE | `features/threads/lib/mastra-timeline.ts:389-397`; `use-mastra-agent.ts:263-294` | **Replay-from-index-0 menduplikasi teks saat reconnect.** Server replay buffer run aktif dari 0 [✅]; reducer tak idempoten (`text-delta` append lagi). Deterministik saat refresh thread Pro mid-run (`committedAgentKind` lite→pro re-subscribe). **[Verif 07-02: deterministik dikonfirmasi via flip agentKind mid-run; skenario blip jaringan = probabilistik. Guard dedup sudah ada utk doc-edit signal tapi TIDAK utk text-delta.]** | Teks jawaban/reasoning dobel; output tool re-append.                                                    | Hitung indeks chunk per-`runId` (seen-counter) atau pakai `observe({runId, offset})`.                                   | approve. ✔️ **DONE 2026-07-02** |
| FE-3  | 🟠 ✅ ✔️DONE | `mastra-chat-thread-surface.tsx:106-129`; `use-mastra-agent.ts:197-199`          | **History basi saat kunjung ulang thread.** Seed dibaca sekali di `useState` initializer; refetch React Query diabaikan; tak ada `staleTime`.                                                                                                                                                                                                                                                                                              | Chat thread A → ke Explore → balik: turn baru hilang sampai hard reload.                                | Gate first-paint pada `isFetching`, atau key komponen pada `dataUpdatedAt`, atau rekonsiliasi server messages ke state. | approve. ✔️ **DONE 2026-07-02** |
| FE-4  | 🟠 ✅ ✔️DONE | `use-mastra-agent.ts:720-724`                                                    | **Stop tak berpengaruh untuk** `/deep`**.** `abort()`→`POST /agents/:id/threads/abort` (abort agent run), bukan workflow run; settle lokal langsung dibatalkan chunk `workflow-step-` berikutnya.                                                                                                                                                                                                                                          | User tekan Stop saat `/deep` → tombol tampak rusak, workflow terus bakar kredit.                        | Wire `run.cancel()` [✅ tersedia] dengan `deepRunRef.current`.                                                          | approve. ✔️ **DONE 2026-07-02** |
| FE-5  | 🟡 ✅ ✔️DONE | `use-mastra-agent.ts:466-474` (catch); `575-687` (re-attach effect)              | **Blip** `/deep` **mid-session → banner error walau run jalan.** Poll `runById` hanya jalan saat mount; catch tampilkan "Gagal memulai riset mendalam".                                                                                                                                                                                                                                                                                    | Progress live & report akhir baru muncul jika user refresh manual.                                      | Ekstrak loop poll dari effect; picu dari catch, atau simpan `deepRunId` state yang effect depend.                       | approve. ✔️ **DONE 2026-07-02** |
| FE-6  | 🟡 ✅ ✔️DONE | `mastra-timeline.ts:488-494`                                                     | **Jawaban terpotong tak bisa dibedakan dari lengkap.** Chunk `error` setelah output apa pun (1 tool row cukup) → `settleAssistantTurn` senyap.                                                                                                                                                                                                                                                                                             | Provider mati mid-jawaban → user dapat setengah jawaban, tanpa banner/retry.                            | Bedakan "error setelah teks jawaban" vs "error setelah hanya tool call"; tampilkan retry.                               | approve. ✔️ **DONE 2026-07-02** |
| FE-7  | 🟡 ✅ ✔️DONE | `mastra-timeline.ts:287-295`; `use-mastra-agent.ts:488`                          | **Race cross-run settle.** `settleAssistantTurn` sasar last-streaming-by-position; `workflow-finish` telat bisa settle placeholder turn BARU → bubble hantu kosong. **[Verif 07-02: butuh aksi user dalam window (reject-plan→send, atau stop→send); mekanisme positional-settle lintas-run persis spt klaim.]**                                                                                                                           | Reject plan lalu kirim pesan baru cepat → ghost message.                                                | Settle by `runId`/`turnId`, bukan posisi.                                                                               | approve. ✔️ **DONE 2026-07-02** |
| FE-8  | 🟢 ✅ ✔️DONE | `use-mastra-agent.ts:689-718`                                                    | **Regenerate turunkan kualitas.** Kirim ulang hanya `lastUserText`; `clientContext` (@mention hydration, ekspansi slash) hilang; `/deep` regen jadi chat biasa + hapus report (orphan `research_sources`).                                                                                                                                                                                                                                 | Hasil regen diam-diam lebih buruk dari asli.                                                            | Re-send `clientContext`; pertahankan tipe turn `/deep` saat regen.                                                      | approve. ✔️ **DONE 2026-07-02** |
| FE-9  | 🟡 ✅ ✔️DONE | `mastra-chat-thread-surface.tsx:164` vs `use-mastra-agent.ts:396-401,677-680`    | **Invalidasi sumber mid-run mati.** `useThreadSources(threadId, !busy && ...)` → query `enabled:false` selama streaming; invalidasi setelah `assign-citations` tak refetch. **[Verif 07-02: stale-mark tetap picu refetch saat busy→false (beat staleTime 10s); kartu live sub-agen tetap muncul via chunk** `workflow-step-output` **— impact user tersoftkan, tapi kartu DB-joined per-turn baru muncul saat settle.]**                  | Kartu sumber "tanpa menunggu workflow-finish" tak pernah terisi mid-run.                                | Buang gate `!busy` (fetch idempoten) / gate pada message count saja.                                                    | approve. ✔️ **DONE 2026-07-02** |
| FE-10 | 🟢 ✅ ✔️DONE | `app/mastra-api/[...path]/route.ts:25-38,93-94`                                  | **Proxy:** `accept-encoding` **diteruskan tapi** `content-encoding` **di-strip; hanya GET/POST di-export.** Streaming core (node:http, `x-accel-buffering:no`, abort) sudah benar. **[Verif 07-02: landmine LATEN — Hono/Mastra server tak kompres by default & semua call site skrg GET/POST;** `thread.update`**(PATCH)/**`thread.delete`**(DELETE) ada di client-js tapi belum dipakai web.]**                                          | Jika ada gzip di depan agent → klien terima byte terkompresi tanpa header. PUT/DELETE masa depan → 405. | Tambah `accept-encoding` ke drop-list; export method lain bila perlu.                                                   | approve. ✔️ **DONE 2026-07-02** |
| FE-11 | 🟢 ✅ ✔️DONE | `features/threads/api.ts:60-76`; `mastra-chat-thread-surface.tsx:151`            | `deep_research` **send-status dead code.** `useSendStatus` dukung `feature:"deep_research"` tapi tak ada call site.                                                                                                                                                                                                                                                                                                                        | User kehabisan kuota `/deep` tak dapat notice; gagal muncul mid-run.                                    | Pre-check kuota `/deep` di composer via feature yang ada.                                                               | approve. ✔️ **DONE 2026-07-02** |
| FE-12 | 🟢 ✅ ✔️DONE | `use-mastra-agent.ts:281-285`                                                    | **Error subscribe di-retry selamanya senyap** (`catch{}` retry 1s, tanpa log / state degraded).                                                                                                                                                                                                                                                                                                                                            | Agent server down → thread tampak idle diam.                                                            | Log + state degraded/banner setelah N retry.                                                                            | approve. ✔️ **DONE 2026-07-02** |

---

## E. Durable background agent + re-attach (Q3)

Semua primitive tersedia di versi terpasang — **tanpa upgrade**: `createDurableAgent`/`createEventedAgent`/`backgroundTasks` (core 1.47 ✅), `subscribeToThread({reconnect})`/`observe({runId,offset})`/`sendMessage`/`queueMessage`/`resumeStream`/`wf.runs` (client-js 1.28 ✅).

| ID    | Prio    | Lokasi                                                                            | Temuan / Gap                                                                                                                                                                                                                                                                                                                                                                                                                                           | Dampak                                                                           | Arah perbaikan                                                                                                      | Keputusan / Saran        |
| ----- | ------- | --------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | ------------------------ |
| DUR-1 | 🟠 ✅   | `defaultAgentThreadPubSub` in-process; `use-mastra-agent.ts` (send path)          | **Chat: buffer & registry run in-process.** Restart agent (deploy/crash) mid-run = run + buffer hilang; reload menampilkan pertanyaan tanpa jawaban/error (disembunyikan FE-12).                                                                                                                                                                                                                                                                       | Turn hilang senyap saat deploy.                                                  | `createDurableAgent`/`createEventedAgent` (cache+pubsub, resumable) → `observe(runId,{offset})` replay dari posisi. | approve                  |
| DUR-2 | 🟠 ✅   | `use-mastra-agent.ts:93-114` (localStorage `aqsha:deep-run:<threadId>`)           | `/deep` **discovery via localStorage.** Premis "tak ada get-run-by-thread" usang: `wf.runs({resourceId, status})` ADA [✅]. **[Verif 07-02:** `ListWorkflowRunsParams` **TAK punya filter** `threadId` **— fix = list** `wf.runs({resourceId, status:'running'})` **lalu match threadId dari run input/snapshot; lookup by-thread langsung memang tak ada.]**                                                                                          | Re-attach gagal lintas device/incognito/storage-cleared; butuh cleanup key basi. | Discovery run aktif dari server via `wf.runs({resourceId, status:'running'})` + match threadId.                     | approve                  |
| DUR-3 | 🟠 ✅   | `use-mastra-agent.ts:575-687` (poll only-at-mount)                                | **Tak ada re-attach in-session** (lihat FE-5).                                                                                                                                                                                                                                                                                                                                                                                                         | Blip mid-`/deep` → tak re-attach sampai reload.                                  | Loop poll/`observe` yang bisa dipicu di luar mount.                                                                 | approve                  |
| DUR-4 | 🟠 ✅   | `use-mastra-agent.ts:720-724`                                                     | **Stop tak durable-aware** (= FE-4).                                                                                                                                                                                                                                                                                                                                                                                                                   | `/deep` tak batal saat Stop.                                                     | `run.cancel()`.                                                                                                     | approve                  |
| DUR-5 | 🟡 ✅⚠️ | `mastra_workflow_snapshot`; `use-mastra-agent.ts:612-616`                         | **Recovery crash: run** `running` **tak resume eksekusi setelah restart.** Snapshot selamat (`runById` OK) tapi poll duduk di snapshot beku. **[Verif 07-02 — KOREKSI mekanisme: "menyerah setelah 8 error (~20s)" HANYA saat** `runById` **throw (server down). Jika server hidup lagi,** `runById` **sukses baca snapshot beku →** `errors` **reset 0 → poll LOOP SELAMANYA (shimmer macet permanen), bukan menyerah. Dua-duanya: run tak resume.]** | Run macet selamanya; sesi tampak selesai padahal tidak.                          | Heuristik "run macet" + affordance `run.restart()/restartAsync()` [✅ tersedia].                                    | approve                  |
| DUR-6 | 🟢 ✅⚠️ | `agent.queueMessage` (unused); `use-mastra-agent.ts:313` (`send` no-op saat busy) | **Follow-up saat run aktif tak didukung.** **[Verif 07-02 — KOREKSI dampak: composer sudah busy-aware (**`canSend = … && !busy`**), submit early-return saat busy → teks TETAP tersimpan di input, tak lenyap. Gap sebenarnya = tak ada queueing, BUKAN message loss.]**                                                                                                                                                                               | User ketik saat busy → tak bisa submit; tak ada antrean follow-up.               | `queueMessage` untuk antre turn berikutnya.                                                                         | approve. need to discuss |
| DUR-7 | 🟢 ✅   | `Mastra` instance (`index.ts:33-45`)                                              | `backgroundTasks` **(butuh storage — sudah ada) belum dipakai.** Subagent riset bisa jadi task persisten yang selamat restart, dipantau `backgroundTaskManager.stream({threadId})`.                                                                                                                                                                                                                                                                    | — (peluang arsitektur durable jangka panjang).                                   | Evaluasi migrasi subagent `/deep` ke background tasks + `untilIdle`.                                                | approve                  |

---

## F. Improvement — system prompt, agent, streaming UX (Q3)

| ID        | Prio | Lokasi                                                            | Temuan                                                                                                                                                                                                                                                             | Manfaat                                                                                                     | Keputusan / Saran        |
| --------- | ---- | ----------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------- | ------------------------ |
| IMP-1     | 🟠   | `instructions.ts`; `agents/*.ts`                                  | Dynamic instructions: suntik tanggal, nama user, tier, ringkasan workspace aktif (lihat CTX-5).                                                                                                                                                                    | Agent kontekstual & sadar-waktu.                                                                            | approve                  |
| IMP-2     | 🟡   | `memory.ts:57`                                                    | Preferensi stabil (bahasa/gaya/format) di working memory `scope:"thread"` → dipelajari ulang tiap thread.                                                                                                                                                          | Suntik preferensi dari profil app via dynamic instructions (hormati keputusan no cross-thread recall).      | approve. need to discuss |
| IMP-3     | 🟡   | `tools/search-*.ts`, `lookup-doi.ts`                              | Deskripsi tool tumpang tindih tanpa "kapan JANGAN dipakai"/routing (arXiv=preprint CS/fisika/math; kesehatan/pendidikan=search_papers; lookup_doi hanya jika sudah punya DOI).                                                                                     | Pemilihan tool lebih tepat. Contoh baik: `verify_identifiers`.                                              | approve                  |
| IMP-4     | 🟡   | `tools/verify-citations.ts:17`                                    | `verify_citations` terima `artifactId`, muat teks server-side (bukan full-text sebagai arg).                                                                                                                                                                       | Hemat token, hindari truncation; buat CFG-1 jadi benar.                                                     | approve                  |
| IMP-5     | 🟡   | `workflows/deep-research.ts:793-806`                              | Langkah citation-verify tak butuh LLM — `assignCitationsStep` sudah punya `{title,doi,arxivId,n}` terstruktur; panggil `CitationService.verifyIdentifiers` langsung.                                                                                               | Deterministik, lebih murah, tanpa drift parsing.                                                            | approve                  |
| IMP-6     | 🟡   | `agents/astra-lite.ts`                                            | Tak ada `errorProcessors` / stream-retry (core punya `stream-error-retry-processor`).                                                                                                                                                                              | 1× 429/502 gateway = turn mati (9 langkah tool hilang). Retry-capable = resilience murah.                   | approve                  |
| IMP-7     | 🟡   | `index.ts:33-45`                                                  | Tak ada observability exporter; processor abaikan tracing context.                                                                                                                                                                                                 | Exporter dasar akan menampakkan CFG-6 (threadId kosong) & bobot token history dari trace prod.              | approve                  |
| IMP-8     | 🟢   | `tools/get-render-payload.ts`                                     | Tak ada range control (kembalikan ~80k char sekaligus).                                                                                                                                                                                                            | Tambah `offset/maxChars` untuk paging PDF panjang.                                                          | approve                  |
| IMP-9     | 🟢   | `workflows/deep-research.ts:749,803`                              | Progress step counter/verify hanya emit di akhir.                                                                                                                                                                                                                  | Emit `status:"searching"` di awal step (seperti `search-sub`) → panel proses jujur saat fase paling lambat. | approve                  |
| IMP-10    | 🟢   | `mastra-chat-thread-surface.tsx:193-213`                          | Tiap `text-delta` rebuild state penuh + memo lookups.                                                                                                                                                                                                              | Batch chunk di `onChunk`, flush per animation-frame (~16-32ms).                                             | approve                  |
| IMP-11    | 🟢   | `mastra-timeline.ts` (default branch)                             | `tool-call-input-streaming-start` di-handle tapi arg delta diabaikan → kartu tool "pop" utuh.                                                                                                                                                                      | Render arg streaming = kartu tool progresif ala Cursor.                                                     | approve                  |
| IMP-12    | 🟢   | `mastra-timeline.ts:548-556` (`WF_STEP_ORDER`)                    | Tak memuat `draft-clarify`/`clarify` → baris klarifikasi hilang dari trace setelah reload.                                                                                                                                                                         | Seed `clarify` saat re-attach.                                                                              | approve                  |
| IMP-13 ⚠️ | 🟢   | `strip-mention-markers.ts:10`; `memory.ts:25`; `astra-lite.ts:62` | Docstring salah/menyesatkan (klaim strip history palsu; "~8 giliran" abaikan bobot tool payload). **[Verif 07-02: 2/3 sub-klaim benar.** `astra-lite.ts:62` **DICORET — komentarnya wajar &** `TokenLimiter` **memang jalan per-step, tak terbukti menyesatkan.]** | Perbaiki agar tak menyesatkan kerja masa depan.                                                             | approve                  |

---

## G. Fitur tambahan agar agent maksimal & useful (Q4)

| ID     | Prio | Area                                | Temuan                                                                                                                                   | Manfaat (mahasiswa akhir / riset ID)                                                                            | Keputusan / Saran        |
| ------ | ---- | ----------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- | ------------------------ |
| FEAT-1 | 🟠   | Tool `read_url`/`read_source`       | Plafon bukti = snippet 1.200 char; `scrapeUrlFirecrawl` ada di `packages/services` (dipakai Explore) tapi tak diekspos sebagai tool.     | Searcher menilai kekuatan bukti hanya dari abstrak → kualitas `/deep` naik signifikan bila bisa baca full-text. | approve                  |
| FEAT-2 | 🟠   | Filter pencarian + sumber Indonesia | `search_papers` hanya `query`+`limit`; OpenAlex native dukung `publication_year`/`language`/sort. Tak ada lane Garuda/SINTA.             | "Jurnal 5 tahun terakhir" belum bisa; mayoritas referensi skripsi berbahasa Indonesia tak terjangkau.           | approve                  |
| FEAT-3 | 🟡   | Tooling daftar pustaka              | Belum ada `format_references`/BibTeX/RIS export. Digabung CTX-8, daftar APA bergantung ingatan model atas metadata yang tak diterimanya. | Daftar pustaka akurat & bisa diekspor.                                                                          | approve. need to discuss |
| FEAT-4 | 🟡   | Tool `verify_statistics`            | `skills/verify-statistics/SKILL.md` suruh statcheck/GRIM "selalu lampirkan hasil hitung ulang" padahal tak ada tool statistik.           | Ship tool statistik, atau tulis ulang skill jadi interpretasi-saja (seperti `replication-readiness`).           | -                        |
| FEAT-5 | 🟢   | `streamUntilIdle` + `queueMessage`  | Keduanya ada di client, tak dipakai (lihat DUR-6).                                                                                       | Follow-up saat busy + agent tetap terbuka lintas background-task.                                               | approve                  |

---

## H. Efisiensi & disiplin tool-calling (concern owner: boros tool call, hasil tak dimanfaatkan)

Konteks concern: agent (lite/pro/deep) sudah pandai memanggil tool, tapi **boros** — memecah 1 kebutuhan jadi banyak pencarian berurutan (cari umur Mr A, lalu cari nama istrinya terpisah, padahal hasil pertama sudah memuat keduanya); Pro bisa ~16 tool call untuk prompt sederhana ("jadwal Piala Dunia 2026"); dan kadang sudah riset tapi jawaban akhir tak memanfaatkannya (malah balik bertanya "tahun berapa?"). Tiga akar: anggaran langkah longgar tanpa nudge berhenti, system prompt tanpa disiplin tool-use, output tool miskin yang memaksa cari ulang.

| ID     | Prio  | Lokasi                                                                                                   | Temuan                                                                                                                                                                                                                                                                                                                                                                                                                                | Dampak                                                                                                                                                            | Arah perbaikan                                                                                                                                                                                                                                                                                                        | Keputusan / Saran        |
| ------ | ----- | -------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------ |
| TOOL-1 | 🟠 ✅ | `agents/astra-lite.ts:54` (Pro `maxSteps:22`, Lite `:46`=10); `processors/ensure-final-response.ts:25`   | **Anggaran langkah longgar tanpa nudge berhenti lebih awal.** `maxSteps` diterjemahkan Mastra jadi `stopWhen=stepCountIs(maxSteps)` — Pro boleh loop sampai 22 langkah. `EnsureFinalResponseProcessor` baru menyuntik "stop, tulis final" di `stepNumber === maxSteps-1` (=langkah ke-22). Tak ada anggaran lunak / pengingat bertahap ("bukti sudah cukup?").                                                                        | Pro nge-loop 16+ tool call untuk prompt sepele; tak ada mekanisme yang membuat model sadar "informasi cukup, tulis jawaban". Nudge datang terlambat (langkah 22). | (1) Turunkan `maxSteps` Pro (mis. 10–12) — 22 terlalu tinggi untuk chat. (2) Nudge bertahap: reminder "nilai apakah bukti sudah cukup; jika ya, jawab" di beberapa checkpoint (mis. step 4/8), bukan hanya step terakhir. (3) Pertimbangkan `isTaskComplete`/scorer atau `stopWhen` komposit [✅ didukung core 1.47]. | approve                  |
| TOOL-2 | 🟠 ✅ | `instructions.ts:16-22` (blok "## Tools")                                                                | **System prompt tak punya disiplin tool-use.** Hanya "Gunakan tool bila relevan, jangan menebak". Tak ada aturan: (a) rencanakan query sebelum menembak; (b) **ekstrak SEMUA fakta relevan dari satu hasil sebelum cari lagi**; (c) gabungkan sub-kebutuhan ke satu query; (d) jangan ulang pencarian yang mirip; (e) **berhenti saat bukti cukup**; (f) utamakan parallel tool call (satu langkah, banyak query) daripada berurutan. | Model cari "umur Mr A" lalu "istri Mr A" terpisah walau hasil pertama memuat keduanya; loop berlebihan.                                                           | Tambah blok "Disiplin riset" ke prompt: rencana→satu batch query paralel→ekstrak maksimal→lanjut hanya jika ada gap nyata→berhenti saat cukup. Beri contoh anti-pola (jangan pecah 1 orang jadi N pencarian).                                                                                                         | approve                  |
| TOOL-3 | 🟠 ✅ | `instructions.ts:24-30` (blok `ask_questions`); `instructions.ts:76` (Pro "~4–5 ronde")                  | **Diskoneksi hasil-tool → jawaban akhir.** Tak ada aturan urutan: model boleh `ask_questions` SETELAH sudah riset (membuang hasil), atau menutup turn dengan pertanyaan tanpa menyertakan temuan parsial. Guidance ask_questions hanya soal "kapan bertanya", bukan "klarifikasi DULU sebelum menghabiskan budget tool, atau sajikan jawaban parsial + tanya".                                                                        | Prompt "berikan jadwal piala dunia" → agent riset → lalu malah tanya "tahun berapa?" tanpa memakai info terkumpul. Kerja tool terbuang.                           | Aturan: klarifikasi ruang lingkup **sebelum** riset berat; jika sudah terlanjur mengumpulkan info, **sajikan yang relevan + ajukan pertanyaan penajam**, jangan buang hasil. "Ambiguitas ringan → asumsikan default + sebutkan asumsi" (sudah ada, tegaskan agar tak jadi pertanyaan).                                | approve. need to discuss |
| TOOL-4 | 🟡 ✅ | `lib/research.ts:87-112` (output) + provider snippet `openalex.ts:154` dll (`trimForSnippet(...,1_200)`) | **Output tool miskin → memaksa cari ulang.** Model hanya menerima `{n,title,url,doi,arxivId,origin,snippet≤1200}`; authors/year/venue dibuang (= CTX-8), tak ada full-text (= FEAT-1). Fakta yang seharusnya "sekali ambil" (umur, relasi, tanggal) sering tak muat di snippet → model menembak query lanjutan.                                                                                                                       | Redundant search; jawaban kurang lengkap; APA author/year dikarang.                                                                                               | Perkaya output (authors/year/venue — CTX-8) + tool `read_url` untuk full-text (FEAT-1). Snippet lebih informatif = lebih sedikit call.                                                                                                                                                                                | approve                  |
| TOOL-5 | 🟡 ✅ | `tools/search-web.ts:19-22`; `tools/search-papers.ts:15-18`                                              | **Tool riset hanya 1** `query` **per call; tak ada batch multi-query.** Untuk beberapa sub-kebutuhan, model harus N call. Tak ada dorongan parallel-tool-call di prompt (OpenAI Responses default paralel, tapi model perlu diarahkan membatch dalam SATU langkah).                                                                                                                                                                   | Sub-kebutuhan terpecah jadi banyak langkah berurutan (makan step budget → memicu TOOL-1).                                                                         | Opsi A: izinkan `queries: string[]` (fan-out internal, satu tool call). Opsi B: instruksikan model mengeluarkan beberapa tool call paralel dalam satu langkah untuk kebutuhan independen.                                                                                                                             | approve                  |
| TOOL-6 | 🟡 ✅ | `agents/astra-lite.ts:78-81` (`defaultOptions`)                                                          | **Hanya** `maxSteps` **yang mengontrol loop — tak ada stop-condition semantik.** Core 1.47 mendukung `stopWhen` komposit, `isTaskComplete` (scorer/policy), `prepareStep` (batasi `activeTools` per fase) [✅ terverifikasi di reference durable-agent]. Tak satupun dipakai.                                                                                                                                                         | Tak ada cara berhenti "karena tugas selesai" selain kehabisan langkah; tak ada penyempitan tool per fase (mis. fase menulis = tools off).                         | Evaluasi `stopWhen`/`isTaskComplete` untuk terminasi berbasis kecukupan; `prepareStep` untuk mematikan tool saat fase sintesis (sejalan `toolChoice:"none"` di `/deep`).                                                                                                                                              | approve                  |
| TOOL-7 | 🟢 ✅ | `model.ts:70` (Pro `reasoningEffort:"high"`); `instructions.ts:70-78` (Mode Pro)                         | **Pro: reasoning** `high` **tiap langkah + instruksi "riset lebih dalam" tanpa pagar kecukupan yang kuat.** "Tetap berhenti saat bukti jenuh" ada tapi lemah vs dorongan kedalaman + budget 22 langkah.                                                                                                                                                                                                                               | Pro cenderung over-explore (banyak call) untuk pertanyaan yang sebenarnya sederhana.                                                                              | Tegaskan "skalakan usaha ke kompleksitas pertanyaan; pertanyaan faktual sederhana = 1 ronde lalu jawab". Pertimbangkan planning-first (satu langkah rencana eksplisit) agar query lebih terarah, bukan lebih banyak.                                                                                                  | approve                  |

**Prioritas dalam concern ini:** TOOL-2 + TOOL-3 (perbaikan prompt, murah & berdampak langsung) → TOOL-1 (turunkan `maxSteps` Pro + nudge bertahap) → TOOL-4 (perkaya output, tumpang tindih CTX-8/FEAT-1) → TOOL-5/6/7.

---

## Urutan yang disarankan

1. **Correctness & keamanan (minggu ini):** ~~SEC-1~~ ✔️ → ~~CTX-2 + CTX-1~~ ✔️ → ~~CFG-1~~ ✔️ → FE-1 → FE-4.
2. **Efisiensi tool-call (dampak cepat, murah):** TOOL-2 + TOOL-3 (prompt) → TOOL-1 (`maxSteps` + nudge) → TOOL-4 (sebagian ✔️ via CTX-8; sisa FEAT-1 `read_url`).
3. **Agent lebih "pintar":** ~~CTX-3 → CTX-5 → CTX-4 → CTX-8~~ ✔️ (section B selesai 2026-07-02) → ~~CFG-4~~ ✔️ (seluruh section C selesai 2026-07-02).
4. **Durability & UX:** FE-2 (`observe({offset})`) → DUR-2 (`wf.runs`) → IMP-10 → FEAT-1/FEAT-2.

## Yang sudah solid (tak perlu aksi)

- Memory terpasang benar per-agent (`lastMessages`, `semanticRecall`, `workingMemory` valid); resourceId server-enforced via `mapUserToResourceId` (menimpa input klien).
- `providerOptions` benar di `defaultOptions` (Mastra 1.47 vNext); `.responses()` (bukan `.chat()`) untuk reasoning summary.
- `ask_questions` suspend/resume cocok dengan `AgentToolExecutionContext`; `EnsureFinalResponseProcessor` pakai API `processInputStep` + `sendSignal` yang benar.
- `chargeToolUsage` idempotency key resume-safe; `reserveChatCitationOffset` serialize `[n]` chat dengan benar; per-subQ RequestContext cloning menghindari race fan-out.
- SSRF guard di `http.ts` menyeluruh; core streaming proxy (node:http, abort propagation, `x-accel-buffering:no`) benar.
